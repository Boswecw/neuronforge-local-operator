# Development / test dependencies. Includes everything in requirements.txt
# plus the test runner and HTTP test client.
#
# Install:  pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8.0
httpx>=0.28             # required by fastapi.testclient.TestClient
