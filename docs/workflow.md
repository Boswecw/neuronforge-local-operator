# Workflow

## Baseline loop
1. place input text in inputs/
2. place prompt file in prompts/
3. run model manually
4. save raw output in outputs/
5. record run in registry/runs.md
6. record observations in notes/log.md
7. score result in evals/ when needed

## Related docs

For the verified wrapper-driven operational workflow, see:

- `docs/operational-workflow.md`

For forward-use conventions for `registry/runs.md`, see:

- `docs/run-registry-conventions.md`
